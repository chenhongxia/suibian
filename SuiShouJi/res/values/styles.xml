<resources>

    <style name="customDialog" parent="@android:style/Theme.Panel">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
   <style name="popWindowAnim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_window_up</item>
        <item name="android:windowExitAnimation">@anim/pop_window_down</item>
    </style>
    
</resources>